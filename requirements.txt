# Quiz Application Dependencies

# NOTE: tkinter is required for the GUI but is NOT installed via pip
# On Linux/Ubuntu: sudo apt-get install python3-tk
# On macOS: tkinter comes with Python installation
# On Windows: tkinter comes with Python installation

# Google Generative AI SDK for question generation
google-genai>=1.56.0

# Environment variable management for API keys
python-dotenv>=1.2.1

# Rich text formatting for CLI output (legacy CLI version)
rich>=13.7.1
